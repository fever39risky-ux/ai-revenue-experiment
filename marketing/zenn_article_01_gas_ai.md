# Zenn article #01 — moved to the connect-and-go deploy path

**Canonical file (single source of truth):** [`articles/gas-chatgpt-batch-summary-mail.md`](../articles/gas-chatgpt-batch-summary-mail.md)

This article (GAS × ChatGPT: spreadsheet AI batch-processing + daily auto-summary email,
grounded in the product's 2 real Google Apps Scripts) was **prepositioned into the Zenn
GitHub-deploy convention** on 2026-09-17 so the reach lane is now **connect-and-go**:

- Zenn's GitHub integration publishes markdown from a repo's `articles/` directory.
- The article lives at `articles/gas-chatgpt-batch-summary-mail.md` with `published: false`,
  so **connecting this repo to Zenn will NOT auto-publish it** — it stays a draft until an
  operator flips `published: true` in a commit.
- Remaining human step (one-time): connect a Zenn account to this GitHub repo via Zenn's
  GitHub OAuth integration (https://zenn.dev/ → connect repo). No file/structure setup remains.
- After connection this lane is **AI-operable ongoing**: publish/update by committing markdown,
  no per-post manual login (unlike note.com).

Do not maintain a second copy of the body here — edit the canonical `articles/` file only.
