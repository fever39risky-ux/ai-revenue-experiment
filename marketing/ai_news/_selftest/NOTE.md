# Renderer proof — NOT real posts

These PNGs are output of `scripts/render_news_card.mjs` using placeholder specs
(`spec_left.json`, `spec_center.json`) to prove the image pipeline renders clean
Japanese 16:9 cards in two layout variants. The content is explicitly marked
「レンダリング確認用サンプル」/「実際のニュースではありません」. They are not
AI-news posts and must not be published.
